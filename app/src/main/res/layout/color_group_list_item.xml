<?xml version="1.0" encoding="utf-8"?>

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="36dp"
        android:layout_marginBottom="6dp"
        android:background="#A0A0A0"
        android:paddingTop="6dp">

        <ImageView
            android:id="@+id/color_group_list_item_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_alignParentLeft="true"
            android:layout_alignParentTop="true"
            android:background="@android:color/transparent"
            android:src="@drawable/painter14" />

        <TextView
            android:id="@+id/color_group_list_item_text"
            android:layout_width="fill_parent"
            android:layout_height="32dp"
            android:layout_toRightOf="@+id/color_group_list_item_icon"
            android:gravity="center_vertical"
            android:paddingLeft="12dp"
            android:text="@+id/color_group_list_item_text"
            android:textColor="#ffffff"
            android:textSize="18dp" />
    </RelativeLayout>